# ## Aptly compose options

# ### names

APTLY_NAME="${APTLY_NAME:-aptly}"
APTLY_TAG="${APTLY_TAG}"
APTLY_HOSTNAME="${APTLY_HOSTNAME:-aptly}"

# ### configs

TZ="${TZ}"

# ### resources

APTLY_HTTP_PORT="${APTLY_HTTP_PORT:-80}"
APTLY_API_PORT="${APTLY_API_PORT:-8080}"
APTLY_VOL="${APTLY_VOL:-aptly-data}"
